# Build outputs
.tmp
graphify-out
.next
out
dist
build

# Dependências (rebuild dentro do container)
node_modules

# Env locais — NUNCA copiar pro container (segredos vivem só no Easypanel)
.env
.env.local
.env.development
.env.production
.env*.local

# Git / IDE / OS
.git
.gitignore
.vscode
.idea
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage / testes
coverage
.nyc_output
# ⚠️ Os testes e os dublês eram COPIADOS pra imagem (o `COPY . .` do builder) e ficavam
#    lá sem nunca executar — peso morto e superfície desnecessária num container de
#    produção. Nenhuma rota os importa, então excluir não muda o build.
**/*.test.ts
**/*.test.tsx
src/test

# Docs internos (não precisam ir pro runtime)
*.md
!README.md
HANDOFF.md
PLAN.md
ROADMAP.md
AGENTS.md
CLAUDE.md
DEPLOY.md

# Claude / config local
.claude
.claude-secret

# Migrations SQL (rodam fora do container, no Supabase)
supabase

# Docker files (não precisa empacotar)
Dockerfile
.dockerignore

# Misc
*.tar.gz
*.zip
